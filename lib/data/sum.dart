class Sum {
  int first;
  int second;
  int third;

  Sum(this.first, this.second, this.third);

  int jumlah() {
    return first + second + third;
  }
}

typedef Jumlah = Sum;
typedef Total = Sum;
